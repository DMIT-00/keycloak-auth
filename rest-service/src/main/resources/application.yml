server:
  port: 8082

spring:
  application:
    name: rest-service
  output:
    ansi:
      enabled: always
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak-auth:4000/auth/realms/Alpha